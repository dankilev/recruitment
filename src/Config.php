<?php

// The DB definitions below are ideally to be stored in a separeate file and imported whenever needed by using require_once
define('DB_HOST', "database"); //this is the host dns address where the database is hosted
define('DB_USERNAME', "testuser"); //username that has credentials to interact with the database
define('DB_PASSWORD', "password"); //the password for the username interacting with the database
define('DB_NAME', "test"); //the name of the database that we are connecting with
define('DB_PORT', 3306); //the port number that accepts connections for the database

?>